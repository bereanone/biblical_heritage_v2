# Progress

## Current Status (August 31, 2026)

### Completed Tasks
- **Archive.org OCR-scan cohort: Contents/tap-to-navigate never worked at all, root-caused and fixed (code + live data repair).** Dean confirmed on-device: opening one of these titles shows a populated Contents/TOC list, but tapping any entry does nothing, and the TOC's entries don't match what's actually displayed — "two different, out-of-sync sources," exactly as suspected. Root-caused to a bug distinct from (and deeper than) the "1 heading total" data-fidelity gap already logged in `BUGS.md`'s 2026-08-31 correction: `supportsCanonicalEpubReader` (`canonical_library_reader.dart`) gates canonical-reader eligibility on an exact `source_type` allowlist that never included `pioneer_archive_org_download`, even though the archive.org installer writes into the identical `ImportedPioneerEpubs` folder as `pioneer_epub_import` and canonicalizes the same way. Every item in this cohort therefore silently fell back to the *legacy* reader on every open — whose Contents comes from `library_navigation_items` (built by the old folder-scanning indexer's own independent EPUB parse), completely disconnected from whatever the legacy renderer actually displays. That's the real source of the two-out-of-sync-pipelines symptom.
  - *Fix, both parts living in the pipeline itself*: (1) added `pioneer_archive_org_download` to `supportsCanonicalEpubReader`'s eligibility check (same folder-path rule already used for `pioneer_epub_import`) so every item in the cohort now routes through the canonical reader, where Contents and reading position both come from the same `library_document_blocks` rows — eliminating the two-source disagreement outright, regardless of how many headings a given title has. (2) Taught `LibraryDocumentCanonicalizer` (version bumped 7→8) to recover chapter headings from the unmarked `"NN - TITLE"` run OCR leaves mid-paragraph (e.g. `"07 - THE FOUR BEASTS Chronological Connection..."`), using the same monotonic-sequence-tracking technique already proven for the roman-numeral `"CHAPTER N."` pattern in the browser-capture importer, so an incidental `"NN - CAPS"` match elsewhere in prose can only be misread as a heading if it lands on the exact next expected chapter number — effectively never.
  - *Two real bugs found only by testing against the actual archive.org files, not synthetic fixtures*: the EPUB nav document itself (`epub:type="toc"`) is the *first* spine entry in these exports and carries its own real `<h2>` page title, which wrongly "proved" the whole book already had semantic heading markup and blocked recovery outright until excluded from that detection scan; and the title-run boundary needed two guards found by inspecting real chapter-adjacent text — a bare number immediately followed by `"-"` is the *next* chapter's own anchor, not a continuation of the current title (was producing `"01 - DANIEL IN CAPTIVITY 02"` instead of stopping at "CAPTIVITY"), and a small set of back-matter markers (APPENDIX/INDEX/PART) end the run even though each is itself a qualifying all-caps word (the last chapter in a pioneer book routinely runs straight into the appendix listing with no boundary at all in the OCR text).
  - *Verified against the real book*: unzipped "Daniel and The Revelation" (`pioneer_uriah_smith_dar1909`, the flagship example from the original report) from the live Library Root and ran the canonicalizer directly against it — went from 1 heading (the book title) to 23: the title plus all 22 real chapters, "01 - DANIEL IN CAPTIVITY" through "22 - THE TREE AND THE RIVER OF LIFE", matching the book's own printed TOC exactly with clean boundaries.
  - *Honest scope check, not assumed*: grepped the `"NN - TITLE"` pattern across every one of the 49 archive.org items that actually have a file present on disk. **Daniel and The Revelation is the only one that uses it.** The other 48 are short tracts/pamphlets/sermons whose original print has no internally-numbered chapters at all, or (confirmed by unzipping and inspecting a sample directly, e.g. "Health, or, How to Live") use a spelled-out convention like "NUMBER ONE"/"NUMBER SIX" that this fix does not attempt. Those 48 correctly remain at a single book-title heading — same as any other legitimately single-section short work elsewhere in the library, not a residual bug. The reader-routing fix is what actually resolves the reported symptom uniformly across all 49 regardless of heading count; the heading-recovery fix is real, live-verified, and will apply automatically to this title and any future import using the same numbered convention.
  - **Live data repair (2026-08-31)**: Backed up `eLibrary.db` (SHA-256 verified, saved to `recovery_backups/20260831_ocr_chapter_heading_repair/`). Re-ran the *existing* `tool/pioneer_archive_org_repair/run_pioneer_archive_org_repair.dart` (its "Step 2" already force-recanonicalizes every surviving archive.org item with a file present on disk, so picking up the new canonicalizer version needed no new tool) dry-run against a scratch copy first, then applied live: 100 items scanned, 49 recanonicalized, 0 failed, 51 left untouched because their source files are still missing on disk under the current Library Root (unchanged, pre-existing gap from the 2026-08-30 repair). `PRAGMA integrity_check: ok` after. Verified by direct query: Daniel and The Revelation now has 23 headings (was 1); all other 48 items' heading counts are unchanged from before, as expected.
  - Added regression tests to `library_document_canonicalizer_generation_test.dart` (OCR recovery on a synthetic multi-page fixture; the real nav-document-exclusion shape; the back-to-back-chapters and APPENDIX title-boundary cases; and a control proving any book with a real semantic heading is never touched) and `canonical_library_reader_epub_pilot_test.dart` (`pioneer_archive_org_download` eligibility, in and outside the managed folder). `flutter analyze` clean project-wide. Full `test/features/library/` + `test/features/utilities/` sweep: 805 tests, exactly one failure — the same pre-existing `elibrary_setup_screen_sections_test.dart` failure already noted below as belonging to unrelated in-progress work in the working tree.
- **Pioneer EPUB imports never built a navigation tree (fixed root cause in the pipeline + live data repaired).** Investigated the report "522 of 524 `pioneer_epub_import_id_*` items have completely empty navigation — zero rows in `library_navigation_items` and zero rows in `library_text_blocks`; only 'The Cross and its Shadow' and 'The Story of the Seer of Patmos' are correctly indexed, via the older `library_item_research_pioneer_*` id scheme." Confirmed directly against the live `BiblicalHeritage/v2/eLibrary.db`: `pioneer_epub_import`/`pioneer_archive_org_download` items had real parsed content in `library_document_blocks`/`library_document_sections` (proving the canonicalizer ran) but zero rows in `library_navigation_items`, while `official_download` items and the two legacy-id Pioneer titles had full navigation coverage (454/454 and 49/1 rows respectively).
  - *Root cause*: two structurally separate pipelines exist for turning a library item's source file into readable content, and only one of them ever writes `library_navigation_items`/`library_text_blocks`:
    1. The old folder-scanning indexer — `CommentaryResearchLibraryService._indexFile` → `_storeNavigationMetadata`/`_storeLibraryTextBlocks` in `commentary_research_library_service_epub_indexing.dart` — walks a designated library folder on disk and builds both the navigation tree and text blocks. This is what every `official_download` item goes through (and, incidentally, what the two legacy-id CIS/SSP rows went through, since they still live under the old `library_item_research_pioneer_*` identity).
    2. The newer canonicalizer — `LibraryDocumentCanonicalizer.canonicalize`, reached via `CanonicalActivation.activate` — only ever writes `library_document_blocks`/`library_document_sections`. It has no navigation-building code anywhere in it (confirmed by grep: zero references to `library_navigation_items` or `library_text_blocks` in `library_document_canonicalizer.dart`).
  - Every Pioneer EPUB import entry point — `import_pioneer_library_screen.dart`'s bulk folder import and `pioneer_thin_zip_install_service.dart`'s combined install (both via `PioneerEpubBulkImportService.prepare` → `LibraryAcquisitionBatchRunner.activate` → `LibraryAcquisitionOrchestrator.prepareExistingEpub` → `CanonicalActivation.activate`), and `pioneer_archive_org_install_service.dart`'s per-title archive.org downloads (calling `CanonicalActivation.activate` directly) — only ever went through pipeline #2. None of them called anything that writes navigation. This is a structural gap in the pipeline itself, not a one-off import mistake — every future Pioneer import would reproduce it identically.
  - `CommentaryResearchLibraryService.indexLocalCatalogedEpubs` (a public method already wired into other screens' "Index Now" flows, e.g. `elibrary_download_screen.dart`) could in principle have been the bridge, but two things kept it from ever reaching Pioneer items: (a) `LibraryCatalogService.countUnindexedManagedItems()` — which decides whether to even show the "pending indexing" prompt — filters to `folder_type IN ('commentary', 'research')`, excluding `folder_type = 'pioneer_epub_import'` outright; and (b) `indexLocalCatalogedEpubs` itself recomputes the target id via `_itemId(folderType, relativePath)` (`library_item_<folderType>_<slug(relativePath)>`), which matches the old scanner's own identity scheme but not Pioneer rows' actual ids (`canonicalLibraryItemId` there is keyed on the EPUB's `dc:identifier` or original source-folder path, not the copied destination path) — calling it against a Pioneer row would have written a second, orphaned `library_items` row rather than fixing the real one (confirmed: exactly one such orphan, `library_item_pioneer_epub_import_importedpioneerepubs_daniel_and_the_revelation_epub`, already existed in the live DB from some prior invocation).
  - *Fix*: Added `CommentaryResearchLibraryService.ensureNavigationIndexed({required Database db, required String libraryItemId, required File file})` in `commentary_research_library_service.dart` — calls the *same*, already-proven `_storeNavigationMetadata`/`_storeLibraryTextBlocks` methods the old indexer uses, but addressed by the caller's exact, already-known `libraryItemId` (never recomputed, never touching the `library_items` row itself — so it can't corrupt Pioneer-specific bookkeeping columns like `pioneer_source_fingerprint`/`pioneer_source_relative_path` the way replaying the old indexer's full `library_items` upsert would have). It's a no-op if navigation already exists for that id (cheap indexed lookup, so retries/redownloads/re-activations cost nothing extra) and it never throws (auxiliary Contents/TOC metadata must never fail a book's activation). Wired it into the two places that cover every current Pioneer EPUB import path: `LibraryAcquisitionOrchestrator.prepareExistingEpub` (the single method behind the batch runner used by both the bulk folder importer and the combined zip install) and `pioneer_archive_org_install_service.dart` (right after its own direct `CanonicalActivation.activate` call, since it doesn't go through the orchestrator). Deliberately did *not* add this to the universal `CanonicalActivation.activate` itself or to the generic retry/redownload/repair call sites — narrower blast radius, and those aren't part of "the Pioneer import pathway" the report was about.
  - **Live data repair (2026-08-30)**: Backed up `eLibrary.db` (SHA-256 verified) before touching anything. Wrote `tool/nav_tree_repair/run_nav_tree_repair.dart` (same `flutter test`-driven pattern as the archive.org repair tool, since the indexer's import graph needs `package:flutter/services.dart`/`path_provider`) — selects any non-deleted, non-`duplicate_retired` EPUB item with real `library_document_blocks` content but zero `library_navigation_items` rows, and runs `ensureNavigationIndexed` against it. Dry run found 468 affected items (419 `pioneer_epub_import`, 49 `pioneer_archive_org_download`); a first apply attempt hit the default 30s `flutter test` timeout after building 128, so added an explicit 15-minute test timeout and re-ran to completion. Final result: **all 421 `pioneer_epub_import` items now have navigation (100%, verified by direct query)**; the 49 `pioneer_archive_org_download` items with content correctly could not be repaired because their source `.epub` files are missing on disk under the current Library Root — the same, already-documented issue from the archive.org repair entry below (not a new bug; `ensureNavigationIndexed` will build their navigation automatically the next time they're successfully re-downloaded, since the fix lives in the activation pipeline itself). The two legacy-id CIS/SSP rows (already correctly indexed) and their `duplicate_retired` shadow copies were confirmed untouched — same nav counts before and after. `PRAGMA integrity_check: ok` after apply.
  - *Also checked, per the report's request*: no correlation found between "direct source EPUB available" vs. "pieced together from other formats" and whether navigation ended up populated — it was 0% for both groups (the uniform `aplib`-sourced bulk import batch, ~99.5% canonicalization success, and the individually-sourced archive.org batch, 49% canonicalization success) for the identical reason: the pipeline never built navigation for either group at all. The archive.org batch's lower canonicalization success rate is the separate, already-tracked "source file missing" issue, not a navigation problem. Also confirmed, purely as a data point (not fixed, not blocking): 79 titles exist as live duplicate rows under two different modern Pioneer id schemes (the `aplib`-sourced bulk import vs. the individual archive.org download) — both copies now have working navigation after this repair, but deduping them is a separate concern left alone as instructed. The 6 EGW pamphlet/misc items flagged `needs_attention` with `index_error: "No readable text content found after parsing"` are a distinct, unrelated bug (official EGW downloads, zero `library_document_blocks` at all — nothing for a navigation builder to work from) and were correctly excluded from this repair's query and left untouched.
  - `flutter analyze` clean on every touched file (`commentary_research_library_service.dart`, `library_acquisition_orchestrator.dart`, `pioneer_archive_org_install_service.dart`, the new repair tool) and on the whole project. Ran `test/features/library/data/library_acquisition_orchestrator_test.dart`, `test/features/utilities/data/pioneer_archive_org_install_service_test.dart`, and `test/features/reader/data/commentary_research_library_service_dual_read_test.dart` directly — all pass. A broader sweep of `test/features/library/` + `test/features/utilities/` surfaced one failing widget test (`elibrary_setup_screen_sections_test.dart`, "install collections section hides empty estimates and uses compact labels") — confirmed by isolating it (reverting just this fix's 3 files back to `HEAD` and re-running) that this failure is **pre-existing in the already-uncommitted working tree from other in-progress work**, not caused by this fix.
- **Pioneer archive.org downloader: every downloaded book was silently corrupted, plus a duplicate-row bug (fixed root cause + live data repaired).** Investigated the user report "fix the Pioneer EPUB import pipeline so it builds navigation and text blocks correctly for all of them, plus dedupe the Daniel Revelation copies" by querying the live `BiblicalHeritage/v2/eLibrary.db` directly. Found two independent bugs, both in the archive.org-backed downloader (`pioneer_archive_org_install_service.dart` — separate from the local-folder bulk importer, `pioneer_epub_bulk_import_service.dart`/`pioneer_epub_folder_inventory_service.dart`, which already had real content):
  1. **Every one of the 101 `pioneer_archive_org_download` items had exactly one `library_document_blocks` row whose text was the raw ZIP bytes of the `.epub` file** (visibly starting with `PK\x03\x04`), with zero navigation. *Root cause*: `_stageActivateAndPromote` activated each download against a **staging** file named `.<title>.epub.<timestamp>.downloading` before renaming it to its final `<title>.epub` destination. `LibraryDocumentCanonicalizer.canonicalize` decides whether to unzip a source purely by `p.extension(source.path)` — and the staging file's real extension was `.downloading`, not `.epub`, so every download was canonicalized as if it were plain text. *Fix*: the staging name now puts the timestamp/hidden marker in front (`.<timestamp>.downloading.<title>.epub`) so the real `.epub` extension survives.
  2. **Duplicate `library_items` rows for one physical file** — confirmed for "Daniel and The Revelation" (Uriah Smith), "The Cross and its Shadow," and "The Story of the Seer of Patmos" (S. N. Haskell), each via a *different* mechanism:
     - DAR: the bundled manifest (`tool/pioneer_epub_download_manifest.json`) lists two real, distinct archive.org editions under the same nominal title (`DAR` and `DAR1909`) whose destination file name is derived from the title alone, so the second download silently overwrote the first on disk while both kept separate `library_items` rows. *Fix*: `install()` now counts title collisions across the whole manifest up front and folds the work's own unique `code` into the file name only for colliding titles (a new public `destinationFileNameFor` helper, unit-tested, plus a manifest-wide collision-safety test).
     - Cross and its Shadow / Seer of Patmos: `pioneer_epub_folder_inventory_service.dart`'s `_legacyLibraryItemIdFor` bridge (meant to link a freshly-scanned file to a pre-existing canonical row instead of minting a new id) excluded any candidate row whose `source_type == 'pioneer_epub_import'` — but the hardcoded canonical bridge targets themselves (`library_item_research_pioneer_stephen_nelson_haskell_CIS`/`SSP`, from `elibrary_legacy_item_id_migration.dart`) also carry that exact source_type once bridged once, since `_copyAndRegister` sets it on every row it touches. So the *first successful bridge poisoned every later re-scan*, which then excluded the very row it should have matched and minted a new `library_item_pioneer_epub_import_id_<uuid>`/`_path_<slug>` duplicate instead. *Fix*: exclude by the scanner's own id-prefix pattern (`library_item_pioneer_epub_import_id_`/`_path_`) instead of by source_type. Added a regression test reproducing the exact failure (a legacy-bridge target whose own row is already `source_type: 'pioneer_epub_import'`) — confirmed it fails without the fix and passes with it.
  - **Live data repair**: backed up `eLibrary.db` (SHA-256 verified) and the running macOS app was quit first. Wrote `tool/pioneer_archive_org_repair/run_pioneer_archive_org_repair.dart` (a `flutter test`-driven tool, not plain `dart run` — the canonicalizer's import graph pulls in `package:flutter/services.dart` via `pioneer_html_capture_folder_scanner.dart`, which the vanilla Dart SDK can't compile) that (a) retires whichever row in a same-physical-file duplicate pair does *not* match this app's stable identity conventions, migrating nothing further since neither side had any user markup/notes/tags, and (b) re-canonicalizes every surviving `pioneer_archive_org_download` item against its real (correctly-named, never affected by the staging bug) destination file with `force: true`. Dry-run tested against a scratch copy first, then a full apply tested against a second scratch copy, then applied live: **3 duplicate pairs retired** (DAR, CIS, SSP — each soft-deleted with `index_status: duplicate_retired`, `PRAGMA integrity_check: ok` after), **49/100 surviving archive.org items recanonicalized** from 1 garbage block each to real content (DAR1909 alone went from 1 to 744 blocks with real section/heading structure), confirmed via direct query afterward.
  - **Known remaining gap, not fixed this pass**: **51 of the 101 `pioneer_archive_org_download` items have no file at their expected relative path under the current Library Root at all** (not a corruption — genuinely missing on disk, all from the 2026-08-20 batch; the 2026-08-28 batch's 50 files are all present). This looks like the CLAUDE.md-documented macOS security-scoped-bookmark reset issue (`library_items` still says `epub_storage_state: 'present'` for all of them) — the files may still exist under a *previous* Library Root folder that's no longer the active bookmark target. The repair tool cannot recover content it can't find, so for these 51 it instead cleared their corrupted generation and set `index_status: 'needs_attention'` with a clear reason, so `LibraryDocumentRepository.isComplete` now correctly reports them as incomplete — the next "Update Verified Pioneer Books" run will re-download them fresh (with the fixed code) rather than skipping them as "already in library" forever (which the old corrupted-but-"complete" generation would otherwise have caused indefinitely). **Dean should check whether a previous Library Root folder still holds these 51 real files** before assuming a full re-download is the only option.
  - Full test suite for touched files: `pioneer_archive_org_install_service_test.dart` (3 new tests: file-name collision resolution, manifest-wide collision safety), `pioneer_epub_bulk_import_service_test.dart` (1 new regression test for the bridge-exclusion bug) — all pass; `flutter analyze` clean on every touched file.
- **NCX Nesting Fix Regressions (both root-caused and fixed, verified against the real archive.org SL27 EPUB)**: The earlier same-day "state-aware token scanner" nesting fix (see below) shipped with two bugs, both in `_parseNcxAnchoredEpubSections` in `pioneer_text_import_service.dart`:
  1. **Depth never decremented.** The token regex captures each navPoint tag as `(/?navPoint\b[^>]*>)` with the leading `<` matched *outside* the capture group, so a closing tag's captured text was `/navPoint>` (one slash) — never `</navPoint>` (two chars). The check `navPointMatch.startsWith('</')` could therefore never be true, so `currentDepth` only ever incremented. Verified on the real SL27 EPUB: computed depths were `0, 2, 4, 6, 7, 9, 11, 13...` instead of `0, 0, 0, 0, 1, 1, 1, 1...`, leaving `parent_id` null for nearly every subsection (only 2 of 16 nav items nested correctly, by coincidence). *Fix*: check `startsWith('/')` instead. Re-verified against the same real EPUB — all 16 items now nest exactly as independently confirmed against whiteestate.org/Apple Books.
  2. **Duplicate/shared-anchor entries could leak a mismatched heading into the wrong section as body text.** When multiple NCX entries collapse onto the same fragment start position (identical anchor, or several anchor-less "whole file" entries in a row), only the last one keeps its content — but the "skip this section's own title if the first paragraph repeats it" check only compared against *that* entry's own title, not the collapsed siblings' titles. Reproduced with a synthetic two-navPoint-one-file fixture: an earlier sibling's real heading text leaked into the winning section's paragraphs, looking exactly like "a run of subsequent heading labels instead of body text." SL27's own current NCX doesn't hit this (each top-level chapter is its own file), but it's the same code path and a real, general bug. *Fix*: collect all titles in the collapsed group (walking back through consecutive same-anchor/path entries) and skip a leading paragraph matching any of them, not just the current entry's own title.
  3. **Ref code duplication.** The new `data-refcode` extraction (see "Refcode Extraction Fix" below) pulls the ref code from a nested marker element such as `<span class="refcode" data-refcode="SL27 12.3">SL27 12.3</span>`, but that span's own visible text was still being left in `plain_text` too (per the "Refcode Stripping Bug" fix), so the same code appeared twice back-to-back wherever both fields are shown together. *Fix*: when the ref code comes from a nested marker element (not the block's own attribute), strip that marker element's markup out of the text before computing `plain_text`, so the ref code is carried once, via the `ref_code` column, and `plain_text` starts with the real body text. (When the refcode is on the block's own attribute rather than a nested visible span, nothing is stripped — there's nothing duplicated to remove.)
  - All three verified via direct execution against the real cached SL27 EPUB and targeted synthetic EPUB fixtures (not just static reading), and the full existing test suite (85 tests) passes with only the one pre-existing, unrelated failure in `library_document_canonicalizer_test.dart` (confirmed present before these changes too).
  - **Still open / not yet regression-tested**: see "Ongoing / Next Steps" below for the multi-title regression pass Dean should run before calling this closed.
- **Refcode Stripping Bug**: Fixed an issue in `pioneer_text_import_service.dart` where visible refcodes (like `<span class="refcode">`) were inadvertently stripped from text during EPUB ingestion. Removed `_removePresentationRefCodeSpans` to preserve the visual tags so they display exactly as authored in CaptureClipper while maintaining consistent database prefixes.
- **Refcode Extraction Fix**: Fixed an issue where ref codes were not being extracted if they were placed on inline `<span class="refcode" data-refcode="...">` elements instead of block elements. The parser now falls back to checking `innerHtml` for the `data-refcode` attribute so `elibrary_ref_index` accurately populates.
- **TOC Content Duplication (and Blank Screen Fix)**: Fixed an issue with nested NCX target elements in EPUB parsing. Parent nodes and child nodes sharing the same target anchor previously caused duplicate content to be extracted. Updated the logic to assign the *same* `spine_index` to all TOC items sharing the same anchor, while only letting the last entry generate the text blocks. This ensures accurate navigation without any duplicate text or blank screens in the viewer.
- **Compilation Error**: Fixed an `Undefined name 'sourceRefCode'` compilation error in `pioneer_text_import_service.dart` by updating an outdated variable reference to `rawSourceRefCode`.
- **Pioneer Import Heading/TOC Extraction Corruption**: Fixed a major bug where body text ("Senator Blair...") was misidentified as headings, the real heading ("ARGUMENT") was misspelled ("ARGUMET"), and TOC hierarchy was lost for Pioneer books like "The National Sunday Law". 
  - *Root Cause*: The NCX parser was prematurely aborting if it found entries without a `#` anchor, falling back to aggressive HTML heading-guessing which falsely flagged short, bolded body paragraphs as headings and flattened the hierarchy. 
  - *Fix*: Replaced the flat regex matching with a state-aware token scanner to accurately compute `depth` for every TOC item. Relaxed the strict requirement that every NCX entry must contain a `#` anchor. Updated the database insertion to use the tracked `depth` property, dynamically establishing the correct `parent_id` foreign keys in the `library_navigation_items` table.

### Ongoing / Next Steps
- **Regression test plan for the NCX fixes above** — re-import each of these into a scratch/test library build and spot-check TOC nesting + spot-check a few paragraphs of body text against a clean source (whiteestate.org, Apple Books, or the printed original):
  1. **The National Sunday Law [SL27]** (A. T. Jones) — the original repro case; confirm ARGUMET/"ARTICLE"/REMARKS/REPLY all nest under "THE NATIONAL SUNDAY LAW", OPEN LETTER nests under APPENDIX A, and Introduction/Contents/Appendix B/Footnotes stay flat top-level items with their own real body text (not each other's headings).
  2. **Daniel and the Revelation** (Uriah Smith, DAR) — large multi-chapter book, good stress test for depth tracking across many top-level siblings.
  3. **The United States in the Light of Prophecy** (Uriah Smith, USLP) — already has an existing unit-test fixture; confirm the live EPUB import still nests correctly.
  4. **History of the Sabbath** (J. N. Andrews, HST) — different author/source lineage than the Jones/Smith titles above.
  5. *(optional, if time allows)* **Christ and His Righteousness** or **The Gospel in Galatians** (E. J. Waggoner) — a different author again, and worth a spot-check for the ref-code fix specifically if either has CaptureClipper-style `data-refcode` spans in its source.
  - For each: verify (a) TOC nesting matches a clean source, (b) no section's body text is actually another section's heading label, (c) no ref code is visibly duplicated back-to-back on any paragraph, (d) no import crash/exception.
- **Pre-existing failing test noticed, not caused by this session's work**: `test/features/utilities/presentation/elibrary_setup_screen_sections_test.dart` — "install collections section hides empty estimates and uses compact labels" fails looking for text "Install eLibrary Collections" (0 widgets found). Reproduces with only the other in-progress uncommitted changes already in the working tree (before this session's nav-tree fix), so it belongs to that other work-in-progress, not to anything in this entry. Whoever is mid-edit on `elibrary_setup_screen.dart`/`elibrary_download_screen.dart`/`library_root_setup_screen.dart` should check it before committing.
- (Awaiting further user request)

**Guardrails for any AI agent working on this file**:
- Make one small, isolated, reviewable change at a time.
- Never delete a function using a line-count-based command (e.g. `sed` with a `+N` range). Always view the full function body first and edit it precisely.
- Show reasoning for why a bug occurs before writing a fix, not just a proposed patch.
- Before treating any apparent text error (misspelling, garbled heading) as an OCR/source artifact, verify against a clean independent source (e.g. whiteestate.org, ellenwhite.org, or Apple Books) — do not assume it's original to the historical text.

---

## How This File Is Used

This file, `Progress.md`, is the shared, ongoing progress and communication log between Dean and whichever AI coding agent (Gemini, Claude Code, etc.) is working on StudyBible at a given time. It is not a one-time snapshot — it is meant to be kept current and replaced/updated as work happens, so that any agent picking up work later has full context without Dean needing to re-explain history.

Expectations for any agent reading or writing this file:
- Read the full file, including prior completed work and any open bug reports, before starting new work.
- When a bug is fixed, move it from an open/new section into "Completed Tasks" with a clear description of the root cause and the fix.
- When a new bug or issue is found (by Dean, by another agent, or by testing), add it as a new dated entry with as much specific detail as possible: the exact book/feature/file involved, what was expected vs. what happened, and any evidence gathered (comparisons to clean sources, screenshots referenced, etc.).
- Keep the file as the single canonical source of current status — avoid letting progress notes live only in chat history or in an agent's own memory, since that context does not carry over between sessions or tools.
- Preserve the guardrails section above across updates; add to it if new failure patterns are observed, but do not remove existing guardrails without Dean's explicit direction.
